using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeachingScript : MonoBehaviour
{
    //    if (Input.GetMouseButtonDown(1)) // on right click
    //        {
    //            Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
    //    RaycastHit hit;
    //            if(Physics.Raycast(ray, out hit, Mathf.Infinity))
    //            {
    //                BlockPlacement.position = hit.point + hit.normal* 0.5f;
    //                BlockPlacement.position = new Vector3(Mathf.Round(BlockPlacement.position.x), Mathf.Round(BlockPlacement.position.y), Mathf.Round(BlockPlacement.position.z));
    //    Instantiate(BlockToPlace, BlockPlacement.position, Quaternion.identity); // spawn the block on our placement position
    //}
    //        }

    private void Update()
    {
        // reset pos
        //if (Input.GetKey("z"))
        //{
        //    transform.rotation = Quaternion.Euler(0, 0, 0);
        //}
        
    }
}
